#include <iostream>
#include <string>
using namespace std;
int main()
{
    int n;
    char input[100];
    cin >> n;
    cin.getline(input, sizeof(input));
    cin.getline(input, sizeof(input));
    cin.getline(input, sizeof(input));
    cin.getline(input, sizeof(input));
    cout << "Ciencia da Computacao" << endl;
    return 0;
}